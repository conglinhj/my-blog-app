import { IntegerNumberDirective } from './integer-number.directive';

describe('IntegerNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new IntegerNumberDirective(null);
    expect(directive).toBeTruthy();
  });
});
